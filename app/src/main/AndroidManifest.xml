<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Location is not optional theatre: since Android 6 the platform gates WiFi scan results
      behind fine location, because a list of nearby BSSIDs is itself a location signal.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- The WiFi stack drops multicast unless a lock is held, which would silently kill mDNS/SSDP. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--
      Required by requestNetwork(), which is what raises a per-app WiFi association. It is a
      normal permission, granted at install, but its absence makes the association request throw
      rather than prompt — which reads exactly like a wrong passphrase.
    -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- BLE reconnaissance. Scanning needs no pairing and no connection. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      Nearby WiFi devices replaces the location gate for scanning on Android 13+.

      Deliberately NOT flagged neverForLocation: that flag asserts the app cannot derive location
      from WiFi data, and the platform honours it by redacting the fields that would allow it —
      RSSI among them, which comes back as 0. A signal survey needs real RSSI, so the assertion
      would be false as well as destructive.
    -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        tools:targetApi="tiramisu" />

    <uses-feature
        android:name="android.hardware.wifi"
        android:required="true" />

    <!--
      802.11mc ranging is a chipset capability, not a software one. Optional so the app still
      installs on handsets without it; the module reports the gap rather than hiding.
    -->
    <uses-feature
        android:name="android.hardware.wifi.rtt"
        android:required="false" />

    <!-- Optional: used to detect an external WiFi adapter on the USB bus. -->
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoidWave">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.VoidWave">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
          The capture service. VpnService is what makes rootless packet capture possible: the
          system hands any app holding BIND_VPN_SERVICE a TUN interface and points the route
          table at it.
        -->
        <service
            android:name=".core.capture.CaptureVpnService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Packet capture for network security assessment" />
        </service>

        <!-- Hands the CA certificate to Android's installer as a content URI. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
